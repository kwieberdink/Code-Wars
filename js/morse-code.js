// Input a string, and get a string in return.





// Filter:
    // Characters should be separated by a single space
    // Words should be separated by a triple space
    // Decline any character other than letters and numbers




// Maybe create a submit button to add the information into a database like the coffee project with (name, roast), like (input, output)






// I want it to automatically update after every letter input
